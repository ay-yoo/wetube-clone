extends base.pug
include mixins/video
block content 
        div.videos__list
            each info in videos
                +video(info)
            else 
                span.empty__message No videos found
