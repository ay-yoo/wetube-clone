extends ../base
include ../mixins/video
block content
    div.profile__data 
        img(src="/"+user.avatarUrl).profile
        h3=user.name
    div.videos__list
        each video in user.videos 
            +video(video)
        else 
            li Sorry nothing found.